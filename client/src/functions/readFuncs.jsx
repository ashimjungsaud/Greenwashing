import { useContractRead } from "@thirdweb-dev/react";


// export const dCampaigns = async () => {
//   const { data, isLoading } = useContractRead(contract, "campaigns", []);
// };

// export const dGetCampaigns = async () => {
//   const { data, isLoading } = useContractRead(contract, "getCampaigns", []);
// };

// export const dGetDonators = async () => {
//   const { data, isLoading } = useContractRead(contract, "getDonators", [_id]);
// };

// export const dGetVotes = async () => {
//   const { data, isLoading } = useContractRead(contract, "getVotes", [_id]);
// };

// export const dHasVoted = async () => {
//   const { data, isLoading } = useContractRead(contract, "hasVoted", []);
// };

// export const dNumberOfCampaigns = async () => {
//   const { data, isLoading } = useContractRead(
//     contract,
//     "numberOfCampaigns",
//     []
//   );
// };

// export const dVotes = async () => {
//   const { data, isLoading } = useContractRead(contract, "votes", []);
// };
